server:
  port: 8092
  servlet:
    context-path: /end-agent

spring:
  application:
    name: end-agent-hierarchy

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

# Agent SDK configuration
agent:
  sdk:
    enabled: true
    logLevel: info
    debug: false

# Network configuration
network:
  udp:
    port: 9012
    bufferSize: 65536
    timeout: 5000

# Logging configuration
logging:
  level:
    root: info
    net.ooder: debug
  file:
    name: logs/end-agent-hierarchy.log
    max-size: 10MB
    max-history: 7